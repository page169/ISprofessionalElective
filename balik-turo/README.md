# Balik-Turo — Filipino Folk Remedy & Herbal Medicine Assistant

IS Professional Elective #4 (Generative AI Systems) — Semester Capstone
**Checkpoint 1: Foundation & Data Pipeline** (Prelim, Performance Task 1)

## What this project is

Balik-Turo ("pass down teaching/knowledge") is a Retrieval-Augmented
Generation assistant that answers questions about traditional Filipino
herbal/folk remedies, grounded in a curated knowledge base of common plants
and preparations used across the Philippines. It is explicitly scoped as a
folk-knowledge reference tool, not a medical diagnostic system — every answer
should point back to traditional practice, not clinical advice.

## Repository structure

```
balik-turo/
├── README.md                     <- you are here
├── data/
│   ├── raw/                      <- 26 raw .txt knowledge base entries (Checkpoint 1 dataset)
│   └── processed/                <- cleaned + tokenized .json output (generated by src/preprocessing.py)
├── src/
│   ├── preprocessing.py          <- Deliverable 3: cleaning, normalization, tokenization
│   └── embedding_demo.py         <- Deliverable 4: embedding generation demo
└── docs/
    ├── project_proposal.docx     <- Deliverable 1: Project Proposal (1-2 pages)
    ├── preprocessing_report.md   <- before/after evidence (auto-generated)
    ├── embedding_report.md       <- embedding model choice + spot-check (auto-generated)
    └── reflection.md             <- Deliverable 5: short reflection
```

## How to run

```bash
# 1. Preprocess the raw dataset
python3 src/preprocessing.py

# 2. Generate sample embeddings
pip install sentence-transformers scikit-learn
python3 src/embedding_demo.py
```

`embedding_demo.py` is written against `sentence-transformers/all-MiniLM-L6-v2`
(the intended production model). If run without internet access to
huggingface.co, it automatically falls back to a local TF-IDF vectorizer
purely so the pipeline is still runnable end-to-end offline — see the
docstring in that file and `docs/embedding_report.md` for details.

## Checkpoint 1 deliverables checklist

- [x] Project Proposal (`docs/project_proposal.docx`)
- [x] Data Collection — 26 raw documents in `data/raw/`
- [x] Text Preprocessing Script (`src/preprocessing.py`) with before/after report
- [x] Embedding Generation Demo (`src/embedding_demo.py`) with model justification
- [x] Reflection (`docs/reflection.md`)

## Roadmap (later checkpoints)

- **Checkpoint 2 (Midterm):** Prompt engineering docs, Chat Completion API
  integration, vector database (Chroma/FAISS) setup, distance metric comparison.
- **Checkpoint 3 (Semi-Final):** Full ingestion pipeline, LangChain/LlamaIndex
  RAG application with conversational memory, live demo.
- **Checkpoint 4 (Final):** PEFT/LoRA rationale, Streamlit/Gradio interface,
  Dockerfile, deployment evidence, final report, and defense.
